package com.example.BookStoreManagementt.repository;


	import org.springframework.data.jpa.repository. JpaRepository;

import jakarta.xml.soap.Detail;
import net.javaguides.detail.entity.Student;

	 public interface bookrepository extends JpaRepository<Detail, Long>{
}
